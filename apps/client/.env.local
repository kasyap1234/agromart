# API Configuration
# Use same-origin relative path so browser requests go through Caddy on localhost:8081
NEXT_PUBLIC_API_URL=/api

# Environment
NODE_ENV=development

# Application
NEXT_PUBLIC_APP_NAME=AgroMart
NEXT_PUBLIC_APP_VERSION=1.0.0
